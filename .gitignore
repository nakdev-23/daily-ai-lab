node_modules
.next
dist
.turbo
*.local
.env
.env.*
!.env.example
.DS_Store
.mcp.json

# Scratch artifacts (screenshots, throwaway chrome profile)
/tmp-*

# Playwright MCP working dir + ad-hoc screenshots
.playwright-mcp/
pw-*.png

# Expo caches / export output
.expo/
.expo-export-test/
